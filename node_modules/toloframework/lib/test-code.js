/**
 * @example
 * var TestCode = require("test-code");
 * var instance = new TestCode();
 * @class TestCode
 */
var TestCode = function(parent, size) {
    
};

// Créer une intance de
// la classe TestCode.
TestCode.create = function() {
    return new TestCode();
};


module.exports = TestCode;

